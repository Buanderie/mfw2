#pragma once

// Internal
#include "node.h"
#include "network.h"

namespace monadic
{
    // A subnetwork is a network that can be used as a node in another network
    class SubNetwork : public monadic::Network, monadic::Node
    {
    public:

    private:

    protected:

    };
}
